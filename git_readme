# To pull the latest version from github

git pull

--------------------------------------------------------

# To push something to main

git add {filename}
git commit -m "message"
git push origin main
(git push)

---------------------------------------------------------
